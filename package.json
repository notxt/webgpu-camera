{
  "name": "webgpu-camera",
  "version": "1.0.0",
  "description": "Learning how to control the camera in a 3D scene",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p client/tsconfig.json",
    "dev": "node server.ts",
    "watch": "tsc -p client/tsconfig.json --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.5",
    "@types/node": "^24.0.10",
    "@webgpu/types": "^0.1.64",
    "playwright": "^1.53.2",
    "typescript": "^5.8.3"
  }
}
